let state = require('./state');

module.exports = (game) => {
  let sprite = state.platform.sprite;
  //game.debug.text(game.time.physicsElapsed, 32, 32);
  //game.debug.body(sprite);
  //game.debug.bodyInfo(sprite, 16, 24);
};
